quarkus.log.level=INFO

# Default base URL for the OpenAI-compatible API (Ollama)
quarkus.agent.base-url=http://localhost:11434

# In integration-tests, FakeLlmClient @Alternative overrides the real client
# To test with real Ollama, use: mvn test -Dquarkus.test.profile=ollama
